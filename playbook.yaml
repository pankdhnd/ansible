- name: Deploy and configure nginx web server
  hosts: local
  tasks: 
  - name: Install nginx server
    become: True
    become_user: root
    apt: 
      name: nginx
      state: latest
  - name: Start nginx server
    become: True
    become_user: root
    service:
      name: nginx
      state: started